# Internal packages

The implementation is split into `lexer`, `ast`, `parser`, `object`, `builtin`,
`eval`, and optionally `repl`. Each package should include tests and
documentation for the behavior it owns.
